<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android" >
    <!-- 实心 -->
    <solid android:color="@color/color_F6F6F6" />
    <!-- 边框 -->
    <stroke
        android:width="0.5dp"
        android:color="@color/color_4E90BF" />
    <!-- 圆角 -->
    <corners android:radius="2dp" />
    <!-- 边距 -->
    <padding
        android:bottom="0dp"
        android:left="0dp"
        android:right="0dp"
        android:top="0dp" />
</shape>